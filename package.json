{
  "name": "esri-rollup-example",
  "version": "0.0.1",
  "description": "Example using rollup to bundle ES6 modules that depend on the ArcGIS API for JavaScript",
  "main": "src/main.js",
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015-rollup": "^1.1.1",
    "replace": "^0.3.0",
    "rollup": "^0.25.8",
    "rollup-plugin-babel": "^2.4.0",
    "semistandard": "^7.0.5",
    "uglify-js": "^1.3.5"
  },
  "scripts": {
    "test": "semistandard \"src/**/*.js\"",
    "build": "rollup -c rollup-config.js && node scripts/postbuild.js && uglifyjs -o dist/bundle.min.js dist/bundle.js",
    "start": "npm test && npm run build"
  },
  "keywords": [
    "ArcGIS",
    "JavaScript",
    "rollup"
  ],
  "author": "Tom Wayson <tom@tomwayson.com>",
  "license": "MIT"
}
